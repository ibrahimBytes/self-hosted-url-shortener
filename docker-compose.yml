services:
  server:
    build:
      context: .
    volumes:
       - db_data_sqlite:/var/lib/trimly
       - custom:/trimly/custom
    environment:
      DB_FILENAME: "/var/lib/trimly/data.sqlite"
    ports:
      - 3000:3000
volumes:
  db_data_sqlite:
  custom:
